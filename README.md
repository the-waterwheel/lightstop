# RAW 测光表

一个只使用主摄、通过 Camera2 获取 RAW_SENSOR 并在 C++ 中分析中央区域的安卓反射式测光表原型。

## 当前实现

- Android 9（API 28）及以上，竖屏启动。
- 右上角按钮是唯一的横竖屏切换入口；ISO、曝光补偿、画幅、变焦和锁止状态不会因切换方向丢失。
- 主摄自动曝光预览；优先选择不超过 1080 级别、且尽量接近传感器比例的输出，设备允许时请求 60 fps。
- 预览对任意胶片画幅都采用保持像素宽高比的中心裁切；竖屏、横屏及方向切换不会把相机缓冲区非等比拉伸。
- 固定主摄，不向 Camera2 提交镜头切换或数码变焦请求。取景变焦由 TextureView 中心裁切完成，RAW 中央 ROI 同比例缩小。
- 支持 135、半格、6×4.5、6×6、6×7、6×9 和 XPan 画幅。
- RAW 不可用时自动改用中央 ISP 预览亮度估算，显示“设备原因可能不准确”的提示，并允许永久关闭该提示。
- RAW 测光按实际传感器捕获 ISO 自适应融合帧数：ISO ≤ 800 使用 3 帧，ISO > 800 使用 5 帧。完整 RAW 使用最多 2 帧的滚动捕获窗口，分析后立即释放，在限制峰值内存的同时让传感器采集与 CPU 分析重叠。
- C++ 按 Bayer 位置扣除逐帧黑电平、按白电平归一化，分别统计 R/G1/G2/B 中位数；Kotlin 使用 Camera2 白平衡增益和颜色矩阵把四个统计值转换为线性 sRGB 亮度（无需解拜尔），再对 3 或 5 帧 EV 做稳健合成。
- 黑白机械仪表界面，仅用少量红色显示基准点、空心测光圈和锁止状态。
- `f` 为黑底白字、`s` 为白底黑字；光圈和快门可分别选择一档、1/2 档或 1/3 档的均匀 EV 刻度。
- 锁定条只在所选档位网格上移动，并使用短促平滑动画过渡；未锁定条严格遵循测光结果连续变化，可停在任意两个标准刻度之间。
- 测光或 ISO/EV 改变时，两条曝光标尺平滑联动，不把测光结果强制舍入成一组刻度。
- 取景框左下角提供单色齿轮“设置”按钮；浅色模式为黑色，深色模式与正文一样为浅灰色。
- Zone 模式支持“按键标点”和“触屏标点”。触屏标点直接在取景画面点击、测光并跟踪；RAW 测光会先从 ISP 预览取得局部特征，再在几何估算位置附近搜索对应 RAW 特征，以减小镜头畸变带来的位置偏差。
- Zone 标点跟踪使用多点相对几何关系；RAW 捕获及 ISP 曝光恢复期间暂时冻结弱纹理点的独立修正，降低低纹理区域与亮度突变时的跳点。
- 支持中文/英文菜单、浅色/深色外观，以及完整的右手/左手操作布局；主测光界面的 `ISO`、`EV`、`f`、`s` 标记不随菜单语言改变。

## 操作

- 转动大拨盘：默认以 1/6 EV 调整曝光补偿。
- 点击拨盘右下角的 `ISO` 小按钮：切换 ISO 调整模式，再转动拨盘即可更改 ISO。
- 点击左上角画幅按钮：在按钮右侧展开胶片画幅列表，再点击需要的画幅。
- 拖动取景区域右侧滑杆：只对主摄画面进行电子裁切。
- 拖动两条标尺右端的单一纵向锁：向上锁定 `f`，向下锁定 `s`。
- 横向拖动当前锁定的标尺：锁定参数按当前档位逐级平滑移动；拖动期间另一条保持静止，松手后依赖条平滑归位到新的精确曝光对应值，并且不会吸附到刻度。
- 按下右下角黑框内的红色空心圆：按实际捕获 ISO 读取 3 或 5 帧 RAW 中央区域并更新曝光对应关系。
- 不支持 RAW 的设备按下测光按钮时会读取 3 帧预览中央区域；结果保留完整的 `f`/`s` 功能，但始终标记为兼容测光。
- 所有非方形画幅始终让长边与屏幕水平轴对齐；XPan 在竖屏和横屏界面中都保持横幅。
- 应用按传感器方向把屏幕画幅换算到物理传感器坐标；由于传感器通常不是正方形，同一横向画幅在竖屏与横屏下会使用不同裁切范围，等效焦距也会同步更新。

## 更多与设置

点击取景框左下角的齿轮按钮进入“设置”界面。当前提供以下设置：

### 测光设置

- 光圈档位：一档、1/2 档、1/3 档。
- 快门档位：一档、1/2 档、1/3 档；与光圈档位相互独立。
- 测光方式：点测光或中央重点测光。点测光读取中心小区域；中央重点使用中心小区域 70% 与较大中心区域 30% 的加权结果，仍不读取画面边缘。
- 标点方式：Zone 模式可选择“按键标点”或“触屏标点”。触屏模式隐藏标点按钮，并将腾出的空间分配给取景区域与记录列表。

### 通用设置

- 语言 / Language：中文或 English，只改变菜单文字。
- 显示模式：浅色或深色。深色模式下整体黑白互换，光圈和快门条均为纯黑底白字。
- 惯用手：右手或左手。左手模式会重新排列取景控制、锁止滑块、测光按钮和波轮，不镜像文字。

### 校准

- “校准”下提供“测光校准”和“暗角矫正”两个独立入口。
- 测光校准支持按设备与镜头分别保存测光偏移。
- 暗角矫正引导用户拍摄亮度均匀的画面，读取单帧 RAW，以中央区域为亮度基准，对二维亮度图进行宽高斯平滑后保存增益图。该图不假设暗角只位于四角，并且仅应用于 Zone 的“触屏标点”RAW 测光。
- 无法输出 RAW 的镜头在校准界面中灰置，并提示“无法输出 RAW，无需校准”（英文界面显示等义英文提示）。

上述设置均保存在应用本地，重启以及横竖屏切换后继续保留。

## 构建

项目需要 JDK 17、Android SDK 34、NDK 25.1.8937393 和 CMake 3.22.1。

```powershell
.\gradlew.bat :app:assembleDebug
```

调试 APK 输出到：

```text
app/build/outputs/apk/debug/app-debug.apk
```

## 测光标定

当前使用 18% 线性 RAW 参考点计算 EV，并将校准偏移按“厂商 + 型号 + camera ID”独立保存。不同手机的 ISO 定义和镜头实际透光率仍会形成固定偏差；Vivo V2405A 主摄已根据同场景参考相机读数标定为 `+1.074 EV`，其他物理主摄默认不套用该偏移，需分别使用灰卡或标准测光表校准。

面向用户的测光校准界面和校准数据管理尚未开发完成；当前菜单中的“测光校准”仅作为预留入口，等待后续开发并实装。
